@model DevIo.App.Models.ProductIndexViewModel

@{
    ViewBag.Title = "Products List";
}
<div class="container">
    <div class="row">
        <div class="mt-3">
            <h2>@ViewBag.Title</h2>
            <a asp-action="Create" asp-controller="Product" class="btn btn-sm btn-secondary">Create</a>
        </div>  
    </div>
    <partial name="_ListProducts" />
</div>

